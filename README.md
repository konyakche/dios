# dios

Lightweight dependency injection and orchestration for Node.js.

Dios is a compact dependency injection container and orchestration toolkit designed to make Node.js services modular and testable. It provides simple registration, resolution, and lifecycle management for services with minimal overhead.

## Features
- Simple service registration and resolution
- Lifecycle hooks for start/stop
- Minimal runtime overhead.
